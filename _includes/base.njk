<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>Fondation Meyer : {{ title }}</title>

  <!-- stylesheet -->
  <link rel="stylesheet" href="/assets/css/main.css">
</head>

<body class="grid" x-data={}>
  <a href="/" class="logo"><img src="/images/fondation-meyer-logo.svg" alt=""></a>

  {% include 'partials/nav.njk' %}

  {% if title != 'beneficiaries' %}
  <h1 class="page-title font-size-900 font-uppercase clr-neutral-900 align-self-end">
    {{ title }}</h1>
  {% endif %}

  <main class="">
    {% block content %}{% endblock %}

    {% if links %}
    <aside>
      <ul role="list">
        {% for link in links %}
        <li class="font-color-primary">
          <a href="{{link.url}}"> {{ link.name }} </a>
        </li>
        {% endfor %}
      </ul>
    </aside>
    {% endif %}
  </main>


  {% include 'partials/footer.njk' %}
</body>

</html>

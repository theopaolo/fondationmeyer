<nav>
  <ul>
    <li>Actions
      <ul>
        {{#each collections.action}} 
          <li><a href="{{ this.url }}">{{ this.data.title }}</a></li>
        {{/each}}
      </ul>
    </li>
    <li>News
      <ul>
        {{#each collections.news}} 
          <li><a href="{{ this.url }}">{{ this.data.title }}</a></li>
        {{/each}}
      </ul>
    </li>
 </ul>
</nav>
